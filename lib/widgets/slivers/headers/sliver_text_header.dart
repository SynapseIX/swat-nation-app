import 'package:flutter/cupertino.dart';

/// Creates a text sliver header.
class SliverTextHeader extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
